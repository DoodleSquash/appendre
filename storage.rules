rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    function isSignedIn() {
      return request.auth != null;
    }

    match /pdf_uploads/{uid}/{fileId} {
      allow read, write: if isSignedIn() && request.auth.uid == uid;
    }
  }
}




